for f in $(find $LIBS_PATH -type d); do
	[ -f $f/funktions ] && . $f/funktions
done

. $LIBS_PATH/colors
. $LIBS_PATH/debug
. $ZSH_PATH/aliases
. $ZSH_PATH/splash
. $ZSH_PATH/options
. $ZSH_PATH/compcfg
. $ZSH_PATH/config
. $ZSH_PATH/bindkeys

eval $(dircolors)


